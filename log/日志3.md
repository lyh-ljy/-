1\. 重构了 Spring Boot 后端项目包结构

2\. 完成了 SysUser 的 Entity → Mapper → Service → Controller 四层代码编写





\- 新建包，包含以下子包：

&#x20; - `entity` — 实体类

&#x20; - `mapper` — MyBatis 数据访问层

&#x20; - `service` — 业务逻辑层

&#x20; - `controller` — 控制器层



&#x20;二、SysUser 四层代码编写



1\. Entity（实体类）

2\. Mapper（数据访问层）

3\. Service（业务逻辑层）

4\. Controller（控制器层）



后来在王老师的帮助下在电脑上装上了新的大模型claude-code

